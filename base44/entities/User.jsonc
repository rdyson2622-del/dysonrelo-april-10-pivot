{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user",
      "description": "User role"
    },
    "portal_role": {
      "type": "string",
      "enum": [
        "client",
        "agent",
        "referral_agent",
        "vendor",
        "hr"
      ],
      "description": "The single portal this subscribed user is assigned to"
    },
    "is_suspended": {
      "type": "boolean",
      "default": false,
      "description": "Whether user account is suspended"
    },
    "suspension_reason": {
      "type": "string",
      "description": "Reason user was suspended"
    },
    "suspension_date": {
      "type": "string",
      "format": "date",
      "description": "Date user was suspended"
    }
  },
  "required": [
    "suspension_reason",
    "suspension_date"
  ]
}