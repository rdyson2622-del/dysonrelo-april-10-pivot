import React from 'react';

/**
 * ReferralFloatingPill — completely disabled/removed per instructions.
 * Renders null so no floating referral widget appears or redirects to dysonrelo.com.
 */
export default function ReferralFloatingPill() {
  return null;
}